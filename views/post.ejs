<h2><%= post.title %></h2>
<p><%= post.body %></p>


comments:
<% comments.forEach((comment) => { %>
  <p>text: <%# comment.body %> whole: <% if(comment.auther){ %>
    <%= comment.auther.name %></p>
    <% } %>
<%}) %>
<form class="" method="post" action="/comments/add">
  <label for="Body">Body
    <input type="text" name="body">
  </label>
    <input type="text" value="<%= post._id %>" name="id">
  <button type="submit" name="button">Add a comment</button>
</form>
<hr>

<a href="/posts">Back to list</a>
